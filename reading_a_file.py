with open ('welcome.txt') as text_file:
  text_data = text_file.read()
  print(text_file)
  print(text_data)
# Path: welcome.txt
# will not work in the code editor since I do not have welcome.txt in the code editor or in the same directory as the code editor